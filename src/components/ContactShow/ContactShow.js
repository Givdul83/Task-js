import React from 'react'
import ShowCaseBase from '../SecondaryShowCaseBase/ShowCaseBase'
import Home from '../../views/Home'

const ContactShow = () => {
  return (
   <ShowCaseBase
                        url = "contacts"
                        pagename = "Contacts"
                        title = "Let's Connect"
                        />
  )
}

export default ContactShow